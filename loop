 *
 * @author ibrahim
 */
public class TestAplication {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
        long mycount;
        mycount = 1000000; 
        
        for (int i=0; i< mycount; i++)
        {
            System.out.println(i);
        }
        // note : loop of 10,000,000 take 12 minutes to complete !! on single processor ordinary thread
        // so loop of 4,000,000,000 (hex ffff ffff) will take 71 hours !!
        }
    
}
